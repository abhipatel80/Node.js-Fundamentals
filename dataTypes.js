//-----------------------------------Primitive data types-----------------------------------
// number
let num = 10;

// string
let str = "Abhi";

// null
let data = null;

// symbol
let symbol = Symbol(10);

// undefined
let a = undefined;
let b;

// boolean
let bool = true;

// BigInt
let bigint = BigInt(10) + BigInt(8);
console.log(bigint);

//-------------------------------non-primitive or Reference data types-------------------------------
// object
const person = { name: "Abhi", age: 20 };

// array
const numbers = [1, 2, 3, 4, 5];

// function 
function greet(name) {
    console.log("Hello, " + name + "!");
}

